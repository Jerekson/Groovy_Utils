// export a node as xml file.
admin.exportXml(asCSNode(CSNode as long)) 

// import manually the file where you want.

// execute the xml file to creat all nodes. 
admin.importXml(asCSNode(ID du dossier « Tools »), asCSNode(ID du nœud correspondant au fichier XML).content.content) 