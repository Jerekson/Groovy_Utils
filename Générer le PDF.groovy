

// Générer le PDF
/*
log.error("Class htmlReport : " + htmlReport.class)
File fileTemp = new File("tempLogFusion.pdf");
BufferedOutputStream writer = new BufferedOutputStream(new FileOutputStream(fileTemp));
writer.write(htmlReport);
writer.flush();
writer.close();

docCreated = docman.createDocument(asCSNode(csvars.REPORT_FOLDER_OUT), reportSuffix, fileTemp)
fileTemp.delete()
*/